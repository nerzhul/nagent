apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: stt

resources:
  - ../../overlays/prod

# Request one NVIDIA GPU per pod and tolerate the GPU node taint.
patches:
  - path: gpu-patch.yaml
    target:
      kind: Deployment
      name: stt-server