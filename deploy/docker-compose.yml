# Local docker-compose for stt-server.
#
# Profiles:
#   - cpu          : no GPU, CPU-only real backend. The image is built with
#                    `--build-arg BACKEND=cpu`. WHISPER_MODEL_PATH still has
#                    to point at a real ggml model — there is no mock here.
#   - gpu-vulkan   : Vulkan GPU passthrough (/dev/dri). BACKEND=vulkan.
#   - gpu-nvidia   : NVIDIA GPU via nvidia-container-toolkit. BACKEND=cuda.
#   - gpu-amd      : AMD GPU via /dev/kfd and /dev/dri. BACKEND=hipblas.
#
# Usage:
#   docker compose --profile gpu-vulkan up
#
# For the cpu profile no GPU libs are needed at build or run time.
# For mock-backend iteration without a model, use `make run-mock` instead.

services:
  stt-server:
    profiles: ["cpu"]
    image: nagent/stt-server:dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BACKEND: cpu
    container_name: stt-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      BIND_ADDR: 0.0.0.0:8080
      WHISPER_MODEL_PATH: ${WHISPER_MODEL_PATH:-/models/ggml-base.bin}
      RUST_LOG: ${RUST_LOG:-info,stt_server=debug,stt_core=debug}
    volumes:
      - ${HOST_MODEL_PATH:-./models}:/models:ro
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  stt-server-vulkan:
    profiles: ["gpu-vulkan"]
    image: nagent/stt-server:dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BACKEND: vulkan
    container_name: stt-server-vulkan
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      BIND_ADDR: 0.0.0.0:8080
      WHISPER_MODEL_PATH: ${WHISPER_MODEL_PATH:-/models/ggml-base.bin}
      RUST_LOG: ${RUST_LOG:-info,stt_server=debug,stt_core=debug}
    volumes:
      - ${HOST_MODEL_PATH:-./models}:/models:ro
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render

  stt-server-nvidia:
    profiles: ["gpu-nvidia"]
    image: nagent/stt-server:dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BACKEND: cuda
    container_name: stt-server-nvidia
    restart: unless-stopped
    runtime: nvidia
    ports:
      - "8080:8080"
    environment:
      BIND_ADDR: 0.0.0.0:8080
      WHISPER_MODEL_PATH: ${WHISPER_MODEL_PATH:-/models/ggml-base.bin}
      RUST_LOG: ${RUST_LOG:-info,stt_server=debug,stt_core=debug}
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    volumes:
      - ${HOST_MODEL_PATH:-./models}:/models:ro

  stt-server-amd:
    profiles: ["gpu-amd"]
    image: nagent/stt-server:dev
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BACKEND: hipblas
    container_name: stt-server-amd
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      BIND_ADDR: 0.0.0.0:8080
      WHISPER_MODEL_PATH: ${WHISPER_MODEL_PATH:-/models/ggml-base.bin}
      RUST_LOG: ${RUST_LOG:-info,stt_server=debug,stt_core=debug}
    volumes:
      - ${HOST_MODEL_PATH:-./models}:/models:ro
    devices:
      - /dev/dri:/dev/dri
      - /dev/kfd:/dev/kfd
    group_add:
      - video
      - render